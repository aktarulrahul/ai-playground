chai_order = dict(type="ginger chai", size="medium", temperature="hot", sugar=True)
print(f"chai_order: {chai_order}")

chai_order["price"] = 100
print(f"chai_order: {chai_order}")

chai_order["price"] = 150
print(f"chai_order: {chai_order}")

chai_order["price"] = 200
print(f"chai_order: {chai_order}")
